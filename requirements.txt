# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1       # ASGI server (includes websockets + http tools)

# ── YouTube extraction ────────────────────────────────────────────────────────
# yt-dlp: primary download engine
yt-dlp>=2026.3.17
# bgutil-ytdlp-pot-provider: PO token provider for yt-dlp
# Includes yt-dlp-get-pot framework + bgutil HTTP/script provider plugins
# The bgutil server (Node.js) is built in Docker and started at runtime
bgutil-ytdlp-pot-provider>=1.3.0
# pytubefix: fallback for metadata
pytubefix>=8.0.0

# ── Async File I/O ────────────────────────────────────────────────────────────
aiofiles==24.1.0

# ── Data Validation ───────────────────────────────────────────────────────────
pydantic==2.10.3
pydantic-settings==2.6.1        # Required for BaseSettings with .env loading

# ── Rate Limiting ─────────────────────────────────────────────────────────────
slowapi==0.1.9

# ── Environment Variables ─────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── HTTP Client (used internally by yt-dlp) ───────────────────────────────────
httpx==0.28.1

# ── Optional: better JSON performance ─────────────────────────────────────────
orjson==3.10.12
